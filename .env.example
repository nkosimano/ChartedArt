# ============================================
# ChartedArt Environment Variables
# ============================================

# --------------------------------------------
# Supabase Configuration (Existing)
# --------------------------------------------
# Public Supabase URL - safe to expose in client
VITE_SUPABASE_URL=https://your-project.supabase.co

# Public anonymous key - safe to expose in client
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ⚠️ IMPORTANT: DO NOT USE VITE_ PREFIX FOR SERVICE ROLE KEY
# Service role key should ONLY be used in backend Lambda functions
# It should be configured in AWS SAM parameters, NOT in .env file

# --------------------------------------------
# Stripe Configuration (Existing)
# --------------------------------------------
# Public publishable key - safe to expose in client
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# ⚠️ IMPORTANT: Stripe secret key should ONLY be in backend
# Configure in AWS SAM parameters, NOT here

# --------------------------------------------
# AWS Configuration (New)
# --------------------------------------------
# API Gateway URL - will be provided after SAM deployment
VITE_API_GATEWAY_URL=https://your-api-id.execute-api.region.amazonaws.com/Prod

# --------------------------------------------
# Google Maps Configuration (Existing)
# --------------------------------------------
# Public API key - safe to expose in client
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-key

# --------------------------------------------
# Monitoring & Error Tracking (Optional)
# --------------------------------------------
# Sentry DSN for frontend error tracking
VITE_SENTRY_DSN=https://your-sentry-dsn

# LogRocket App ID for session replay
VITE_LOGROCKET_APP_ID=your-app-id

# --------------------------------------------
# Environment
# --------------------------------------------
# development | staging | production
VITE_ENVIRONMENT=development
